{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome against localhost",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8000",  // adjust if different
      "webRoot": "${workspaceFolder}"
    }
  ]
}
